const expect = require("chai").expect;
const app = require("../../app");
//const config = require("../../config");

//`${config.api.prefix}/user`;

describe("Prolinkup API", () => {
  describe("USER ==> Signup", () => {
    it("OK, Create user", async () => {});
  });
});
